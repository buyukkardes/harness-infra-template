# Getting started

1. Configure `harness.config.yaml`
2. Copy product spec → `specs/SPECS.md`
3. `git init && npm ci && bash scripts/verify.sh`
4. Fresh chat + `harness/session-prompt.md` per unit

Product spec example: **task-notes-spec** repository.
