project:
  name: "task-notes-replay"
  description: "Task Notes API replay via harness-infra-template"

stack:
  profile: node-ts

verify:
  install: "npm ci"
  test: "npm test"
  build: "npm run build"

github:
  default_branch: main
