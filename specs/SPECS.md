# SPECS — replace with your product spec

This file is a **placeholder**. Delete and copy your product `SPECS.md` here.

## Phase 0 — Project setup (minimal)

- [ ] Set `harness.config.yaml` (name, description)
- [ ] `git init`, initial commit
- [ ] Copy product spec from external repo (e.g. task-notes-spec)

**Verification:** `bash scripts/init.sh` OK; product spec present.

---

*(Product phases start in your copied spec — Phase 1+)*
